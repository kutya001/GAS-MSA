# Change Logs — МобилТрек Pro

> Журнал всех изменений проекта. Заполняется **при каждом изменении**, включая незначительные правки.

| Дата | Описание | Анализ | To Be (Что должно быть) | As Is (Что сейчас) | Что изменено | Файлы |
|------|----------|--------|--------------------------|---------------------|--------------|-------|
| 2026-04-08 | Создание структуры Artifacts и банка знаний | Проект не имел документации и системы отслеживания контекста для ИИ | Организованная папка Artifacts с Documents и memory-bank; инструкция с правилами обновления | Документация отсутствовала, FSD лежал в корне | Создан `Artifacts/Documents/`, `Artifacts/memory-bank/` с 4 файлами; FSD перемещён; инструкция обновлена | - `Artifacts/Documents/FSD MDM Интеграция.md`<br>- `Artifacts/memory-bank/FSD.md`<br>- `Artifacts/memory-bank/productContext.md`<br>- `Artifacts/memory-bank/activeContext.md`<br>- `Artifacts/memory-bank/systemArchitecture.md`<br>- `.github/instructions/gas-project.instructions.md` |
| 2026-04-08 | Создание журнала изменений (Change Logs) | Отсутствовал единый лог изменений для отслеживания истории правок | Файл Change_Logs.md с таблицей; правило в инструкции обновлять лог при каждом изменении | Лог изменений не вёлся | Создан `Artifacts/memory-bank/Change_Logs.md`; обновлена инструкция | - `Artifacts/memory-bank/Change_Logs.md`<br>- `.github/instructions/gas-project.instructions.md` |
| 2026-04-08 | Формат столбца «Файлы» в Change Logs | Файлы записывались в одну строку — трудно читать | Каждый файл с новой строки через `<br>` и префикс `- ` | Файлы перечислены через запятую в одну строку | Обновлён формат записей в Change_Logs.md; добавлено правило формата в инструкцию | - `Artifacts/memory-bank/Change_Logs.md`<br>- `.github/instructions/gas-project.instructions.md` |
| 2026-04-08 | Редизайн Закупок/Продаж: MDM-интеграция, 4-блочные формы | Закупки хранили brand_id/model_id/spec/color напрямую, нарушая MDM-архитектуру; характеристики товаров дублировались | Закупки хранят product_id → MDM_Номенклатура; формы разделены на 4 логических блока (Основная инфо, Классификация, Характеристики из шаблона, Оплаты); оплаты поставщику в отдельной таблице ОплатыЗакупок | brand_id, model_id, spec, color хранились в таблице Закупки; оплата поставщику — одно поле pay_wallet_id; характеристики не связаны с MDM-шаблонами | Удалены brand_id/model_id/spec/color из Закупки, добавлен product_id; добавлена таблица ОплатыЗакупок; Purchases.js переписан с MDM-интеграцией (getPurchasePayments, addPurchasePayment); Sales.js обновлён (product_name вместо brand/model); формы PagePurchases и PageSales на 4 блока с container-полями; FormEngine расширен типом container; аналитика переведена на class_id; моки и loadMasterData обновлены | - `Purchases.js`<br>- `Sales.js`<br>- `Analytics.js`<br>- `PagePurchases.html`<br>- `PageSales.html`<br>- `PageWarehouses.html`<br>- `ScriptHelpers.html`<br>- `Styles.html`<br>- `FormEngine.html`<br>- `Config.js`<br>- `DBinit.js` |
| 2026-04-08 | Визуальные островки (island blocks), IMEI-поиск, зеркальная форма Продаж | Формы Закупок/Продаж использовали плоские разделители (mf-gh), не имеющие визуального выделения блоков; форма Продаж не зеркалила Закупки; не было поиска IMEI; прибыль не рассчитывалась | FormEngine рендерит group-поля как карточки (mf-block) с скруглёнными углами и тенью во всех режимах; форма Продаж — 4 блока (Документ, Товар с IMEI-поиском, Характеристики из закупки, Оплата); себестоимость и прибыль автоподтягиваются; DT-таблица продаж показывает себестоимость, цену и прибыль | group-поля рендерились как inline-разделители `<div class="mf-gh">`; форма Продаж — 2 блока (Товар+Покупатель, Финансы) с обычным select purchase_id; нет IMEI-поиска; нет прибыли; getSales не возвращал imei/cost/profit/template | FormEngine: _viewHtml/_editHtml разделяют поля на блоки по group-маркерам, каждый оборачивается в `<div class="mf-block">`; _fieldHtml(group) → пустая строка; Styles: добавлены .mf-block/.mf-bh/.imei-srch/.imei-dd/.imei-opt CSS, .mf-vb/.mf-eb → background:var(--bg); Sales.js: getSales возвращает imei, has_imei, cost_kgs, cost_usd, profit_kgs, wh_name, template_id, attribute_values из purchase; PagePurchases: реорганизация на Block 0 (Документ) / Block 1 (Товар с IMEI+Classification+Cost) / Block 2 (Характеристики) / Block 3 (Оплаты); PageSales: полная перестройка — IMEI-поиск контейнер с dropdown+autocomplete, auto-fill cost/profit, template attrs view-only из закупки, 4 блока зеркало закупок; DT cols: +cost_kgs +profit_kgs; ScriptHelpers: мок getSales обновлён с новыми полями | - `FormEngine.html`<br>- `Styles.html`<br>- `Sales.js`<br>- `PagePurchases.html`<br>- `PageSales.html`<br>- `ScriptHelpers.html` |
| 2026-04-08 | Fix showIf в _viewHtml + MDM-дерево 3 уровня | 1) FormEngine._viewHtml() не проверяла showIf → в Справочниках view-режим показывал brand_id/class_id/type_id для всех вкладок; 2) Дерево шаблонов MDM имело 4 уровня (Класс→Тип→Назначение→Шаблон), но Назначение=Шаблон, лишний уровень вложенности | 1) _viewHtml проверяет showIf аналогично _editHtml; 2) Дерево шаблонов 3 уровня: Класс→Тип→Назначение=Шаблон (📄 leaf); дерево номенклатуры — аналогично 3 уровня | 1) showIf не вызывался в view-режиме; 2) Назначение рендерилось как папка (📂) с вложенными шаблонами (📄) внизу — 4 уровня | FormEngine._viewHtml: добавлена проверка `if (f.showIf && !f.showIf(self)) return;`; PageMDM._mdmRenderTemplates: Назначения → leaf-ноды (📄), клик открывает шаблон или создаёт новый через _mdmCreateTplForPurpose; _mdmRenderProducts: убран 4-й уровень, таблицы продуктов для назначений рендерятся на уровне типа; mdmOpenTplModal: isEdit проверяет tpl.id вместо tpl | - `FormEngine.html`<br>- `PageMDM.html` |
| 2026-04-08 | Удаление Ref_Назначения — полная архитектурная ревизия | Ref_Назначения (Назначение) был избыточным 3-м уровнем классификации. Шаблоны MDM уже выполняют роль «назначения» — каждый шаблон определяет, что есть товар. Двойная абстракция (Назначение + Шаблон) усложняла каскады, формы и дерево без пользы | Иерархия Класс → Тип (2 уровня справочников), шаблоны = 3-й уровень MDM. Таблица Ref_Назначения удалена, purpose_id удалён из всех таблиц и модулей. Каскады упрощены: MDM шаблоны — Класс/Тип; Продукты — Класс/Тип/Шаблон; Закупки — Класс/Тип | Иерархия 3 уровня: Класс → Тип → Назначение. Ref_Назначения в SCHEMA/SH/seed. purpose_id в Закупки, MDM_Шаблоны. Все фронтенд-формы имели каскад с Назначением | **Бэкенд:** DBinit.js — удалена схема Ref_Назначения, purpose_id из Закупки и MDM_Шаблоны; Config.js — удалён PURPOSES из SH; Refs.js — удалён purposes из REF_MAP, getRefData, addRef, updateRef; MDM.js — удалён purpose_id из getTemplates, saveTemplate, getMDMContext; Purchases.js — удалён prMap, purpose_id из маппинга/фильтров/add/update; Sales.js — удалён prMap, purpose_id/purpose_name из маппинга. **Фронтенд:** PageRefs.html — удалена вкладка «Назначения», поле type_id, колонка type_name; PageMDM.html — удалён select mdmTplPurpose, переписано дерево шаблонов (2 уровня Класс→Тип→📄Шаблон), удалены _mdmFillTplPurposes/_mdmProdOnPurposeChange, упрощены каскады, удалён purpose_id из JSON-схемы/экспорта/импорта; PagePurchases.html — упрощён _findPurchaseTemplate(2 параметра), удалено поле purpose_id и каскад, упрощены afterMount; PageSales.html — удалён purpose_name из 2 path-отображений; ScriptHelpers.html — удалены purposes из ST.refs/ST.mdm, мок-массив purposes, purpose_id/purpose_name из всех мок-строк | - `DBinit.js`<br>- `Config.js`<br>- `Refs.js`<br>- `MDM.js`<br>- `Purchases.js`<br>- `Sales.js`<br>- `PageRefs.html`<br>- `PageMDM.html`<br>- `PagePurchases.html`<br>- `PageSales.html`<br>- `ScriptHelpers.html` |
| 2026-04-08 | Сворачиваемый сайдбар | Сайдбар всегда занимал фиксированную ширину (220 px), на узких экранах оставлял мало места контенту | Сайдбар сворачивается до 56 px: остаются только иконки; текст и секции скрыты. Кнопка-шеврон в подвале сайдбара; состояние сохраняется в localStorage | Сайдбар фиксированной ширины 220 px, без возможности свернуть | CSS: `.sb.collapsed` (width 56px, скрытие `.sb-txt`/`.sb-sec`, центровка иконок), `.sb-toggle` кнопка, `transition: margin-left` на `.main`. JS: `toggleSidebar()` переключает класс и `--sbw`, `applySidebarState()` восстанавливает из localStorage; вызов из `init()`. HTML: кнопка-шеврон в `.sb-foot` рядом с darkmode-переключателем | - `Styles.html`<br>- `Frontend.html`<br>- `ScriptHelpers.html` |
| 2026-04-08 | Динамическая валюта вместо захардкоженного ₸ | Все суммы отображались с суффиксом `₸` (тенге), хотя система поддерживает разные валюты через Настройки. Невозможно сменить валюту отображения без правок кода | Глобальная переменная `CUR` (по умолчанию `'KGS'`) подтягивается из настроек (`base_currency`) при `loadMasterData()`; все 60+ мест отображения сумм используют `CUR` вместо литерала `₸` | Во всех страницах суммы отображались с литералом `₸`: KPI, таблицы, тосты, валидации, графики | ScriptHelpers: добавлен `var CUR='KGS'`; `loadMasterData()` вызывает `api('getSettings')` и устанавливает `CUR=settings.base_currency`. Заменены все `₸` → `CUR` во всех страницах (60+ вхождений) | - `ScriptHelpers.html`<br>- `PageDashboard.html`<br>- `PagePurchases.html`<br>- `PageSales.html`<br>- `PageAnalytics.html`<br>- `PagePayments.html`<br>- `PageDebts.html`<br>- `PageWarehouses.html`<br>- `Payments.js` |
| 2026-04-08 | Каскад «Назначение → Номенклатура» в форме Закупок | Форма закупки не позволяла явно выбирать шаблон (назначение); шаблон подбирался автоматически по class_id+type_id через `_findPurchaseTemplate`, не давая пользователю контроля при множестве шаблонов; поле product_id заполнялось через один каскад | Новые поля `template_id` (label «Назначение», select) и `product_id` (label «Номенклатура (MDM)», select). Каскад: Класс→Тип→Назначение→Номенклатура. Помощники `_pUpdateTemplates(form)`, `_pUpdateProducts(form)` фильтруют варианты. `_findTemplateById(tplId)` вместо `_findPurchaseTemplate(class,type)` | Шаблон определялся скрыто через `_findPurchaseTemplate(classId, typeId)` — возвращал первый совпавший; product_id заполнялся без промежуточного выбора шаблона | Удалён `_findPurchaseTemplate`; добавлен `_findTemplateById(tplId)`; добавлены хелперы `_pUpdateTemplates(form)`, `_pUpdateProducts(form)`; добавлены поля template_id, product_id в массив fields; onChange для class_id/type_id вызывают `_pUpdateTemplates`; afterMount/onSave обновлены для каскада | - `PagePurchases.html` |
| 2026-04-08 | Режим IMEI / ручной выбор в форме Продаж | Форма продажи работала только через IMEI-поиск; невозможно было продать товар без IMEI (qty>1); отсутствовал ручной выбор класса/типа/шаблона в продажах | Чекбокс `has_imei`: IMEI-режим (поиск IMEI → авто-заполнение и блокировка Class/Type/Template) или ручной режим (каскад Class→Type→Template, редактируемые характеристики). `_sAutoFillSelects(pur)` — автозаполнение при IMEI; `_sUpdateBlock2Manual(tpl)` — характеристики при ручном режиме | Форма всегда показывала IMEI-поиск; Class/Type/Template не было; невозможна продажа без IMEI | Добавлен чекбокс `has_imei` с onChange переключающим видимость IMEI-контейнера и disabled-состояние select'ов. Добавлены поля class_id, type_id, template_id с showIf/disabled. Добавлены `_sAutoFillSelects(pur)`, `_sUpdateTemplates(form)`, `_sUpdateBlock2Manual(tpl)`. onSave обработка двух режимов. afterMount инициализация каскадов | - `PageSales.html` |
| 2026-04-08 | Исправление сворачиваемого сайдбара — иконки | При сворачивании сайдбара оставались видны текстовые метки пунктов меню вместо только иконок — CSS `.sb.collapsed .sb-item` не скрывал текст | При сворачивании видны только иконки (SVG); текст скрыт через `font-size:0;gap:0`, размер иконок зафиксирован `width/height:18px` | Текст пунктов оставался видимым при `collapsed` — `font-size` и `gap` наследовались от обычного состояния | CSS: `.sb.collapsed .sb-item{font-size:0;gap:0}`, `.sb.collapsed .sb-item svg{width:18px;height:18px}` | - `Styles.html` |
| 2026-04-08 | Авто-заполнение характеристик из номенклатуры в Закупках | При выборе продукта из MDM (номенклатуры) блок «⚙️ Характеристики товара» оставался пустым; характеристики не подтягивались автоматически, хотя они уже заданы в номенклатуре | При выборе продукта `product_id` его `attribute_values` автоматически подтягиваются в блок характеристик через `_pAutoFillAttrs(form)` | Блок характеристик заполнялся только при смене шаблона (`_pUpdateBlock2`) — выбор конкретного продукта не обновлял значения атрибутов | Добавлен `onChange` на поле `product_id` вызывающий `_pAutoFillAttrs(form)`. Новая функция `_pAutoFillAttrs`: находит продукт по ID, берёт его `attribute_values` и передаёт в `_pUpdateBlock2(form, vals)` | - `PagePurchases.html` |
| 2026-04-08 | Рассрочка в форме Продаж + валидация showIf | Блок «💰 Оплата» всегда требовал выбора кошелька и суммы оплаты; невозможно продать товар в рассрочку (оплата позже, частями). FormEngine проверял required-поля даже если они скрыты через showIf | Чекбокс `is_installment` («📅 Рассрочка»): при включении поля wallet_id и paid_kgs скрываются и не обязательны; долг = полная цена. FormEngine._save() пропускает required-валидацию для полей с `showIf=false` | Кошелёк и оплата были обязательны при каждой продаже; невозможно отложить оплату. FormEngine валидировал все required-поля без учёта showIf | PageSales: добавлен чекбокс `is_installment` в Block 3 с `onChange: refreshVis()` и renderView с badge; `showIf: !is_installment` на wallet_id и paid_kgs; `_sCalcDebt` учитывает isInstallment (paid=0); onSave: isInstallment → paid=0, wallet_id=null. FormEngine._save: добавлена проверка `if (f.showIf && !f.showIf(self)) return false` в фильтр missing-полей | - `PageSales.html`<br>- `FormEngine.html` |
| 2026-04-08 | Капитальный рефакторинг FormEngine — UX-улучшения | 1) Форма закрывалась по drag (mousedown внутри + mouseup на overlay = click на overlay). 2) Нет индикации режима формы. 3) При закрытии формы с несохранёнными данными — данные терялись без предупреждения. 4) Форма ждала API перед закрытием. 5) Toast-уведомления внизу-справа. 6) Кнопки не стандартизированы | 1) Drag-safe overlay (mousedown+mouseup проверяют что оба на overlay). 2) Цветной бейдж режима: Создание(зелёный)/Редактирование(оранжевый)/Просмотр(синий). 3) Снимок `_initSnap` значений при открытии; `_hasChanges()` сравнивает; `tryClose()` подсвечивает изменённые поля (.mf-changed) + диалог (Отменить/Сохранить). 4) `_save()` закрывает форму сразу, `onSave` выполняется фоном. 5) Toast вверху-справа. 6) View: 🗑Удалить\|spacer\|Закрыть\|✏️Редактировать; Edit/Create: Отмена\|💾Сохранить | 1) `el.addEventListener('click')` на overlay. 2) _title() добавлял текстовый префикс, но не бейдж. 3) Нет отслеживания изменений. 4) `_save()` ожидал `await onSave()` → затем `close()`. 5) `.toast-wrap{bottom:16px}`. 6) Edit-кнопка «Отмена» возвращала в view вместо закрытия | **FormEngine.html**: добавлены `_initSnap`/`_ovMouseTarget` в constructor; `tryClose()` — публичный метод с dirty-проверкой; `_hasChanges()`/`_getChangedKeys()` — сравнение со снимком; `_highlightChanged()` — подсветка .mf-changed; `_showUnsavedDialog()` — оверлей .mf-unsaved-ov с кнопками; `_onCloseBtn()` → tryClose; `_ensureBuilt()` — mousedown/mouseup вместо click; `_render()` — бейдж .mf-badge + setTimeout для _initSnap; `_title()` — убраны текстовые префиксы режима; `_footHtml()` — стандартные кнопки; `_save()` — close() перед await onSave(); `_delete()` — close() перед await onDelete(). **Styles.html**: `.toast-wrap{top:16px}` вместо bottom; добавлены `.mf-badge`/`.mf-badge-create/edit/view`, `.mf-changed`+`@keyframes mfPulse`, `.mf-unsaved-ov`/`.mf-unsaved`/`.mf-unsaved-ico/title/msg/btns`. **ScriptHelpers.html**: удалён глобальный `document.addEventListener('click')` overlay handler; Escape-handler роутит через `MF_INST[id].tryClose()`. **PageSales.html**: валидации IMEI/paid из onSave → validate callback. **PagePayments.html**: валидация sale_id из onSave → validate callback | - `FormEngine.html`<br>- `Styles.html`<br>- `ScriptHelpers.html`<br>- `PageSales.html`<br>- `PagePayments.html` |
| 2026-04-08 | FormEngine async validate + IMEI uniqueness + моментальная оплата закупок | FormEngine.validate() принимал только синхронный результат, проверка IMEI уникальности отсутствовала, нельзя было оплатить поставщика при создании закупки | FormEngine._save() ждёт Promise от validate(); checkImeiUnique() серверная проверка; addPurchase/updatePurchase проверяют IMEI; addPurchase принимает pay_wallet_id/pay_amount → создаёт PurchasePayment+CashOp+adjustBalance; updateSale/getSalePayments — новые серверные API | validate() только sync; IMEI-дубликаты возможны; оплата поставщику только через отдельный блок оплат после создания; нет редактирования продажи; нет getSalePayments | **FormEngine.html**: `_save()` — `if (err && typeof err.then === 'function') err = await err;`. **Purchases.js**: `checkImeiUnique(p)`; IMEI-проверка в `addPurchase` и `updatePurchase`; блок immediate payment в `addPurchase` (pay_wallet_id/pay_amount → PurchasePayment + _findOrCreateCat + _appendCashOp + _adjustBalance). **Sales.js**: `updateSale(p)` — обновление buyer/wa/sale_date/manager_id/total_kgs/note, пересчёт debt_kgs; `getSalePayments(p)` — фильтрация Оплаты по sale_id с wallet_name | - `FormEngine.html`<br>- `Purchases.js`<br>- `Sales.js` |
| 2026-04-08 | PagePurchases: рассрочка, inline оплата при создании, платёжный блок с валидацией, IMEI validate | Block 3 не имел рассрочки; нельзя оплатить при создании; _renderPurchasePayments не проверял fully-paid; нет IMEI front-валидации | Block 3: supplier_installment checkbox (create-only); pay_wallet_id/pay_amount (create-only, showIf=!installment); _pCalcKgs автозаполняет pay_amount; _renderPurchasePayments(id,purchaseId,costKgs) — fully-paid badge, remaining alert, disabled add-form when paid; _addPurchasePayment — cost validation; validate — async IMEI через checkImeiUnique; onSave — pay params при создании; afterMount — costKgs в payments | Block 3 содержал только payments_block container без рассрочки/inline оплаты; _renderPurchasePayments принимал mode вместо costKgs; _addPurchasePayment без cost-валидации; validate — только формат IMEI; onSave без pay_wallet_id/pay_amount | **PagePurchases.html**: `_pCalcKgs` — добавлено автозаполнение pay_amount; fields[] — supplier_installment (checkbox, create-only, showIf: _mode=create), pay_wallet_id (select, create-only, showIf: create && !installment), pay_amount (number, create-only, showIf: create && !installment); `validate()` — async IMEI check через api('checkImeiUnique'); `onSave()` — передача pay_wallet_id/pay_amount при !editId && !installment; `afterMount()` — _renderPurchasePayments(id, rec.id, rec.cost_kgs) для view/edit | - `PagePurchases.html` |
| 2026-04-08 | PageSales: редактирование продаж, inline таблица оплат, edit mode | Продажи нельзя редактировать (canEdit:false); Block 3 view — обычные поля; нет inline таблицы оплат; viewActions — отдельная кнопка «Внести оплату» | canEdit/viewActions удалены → кнопка «Редактировать» доступна; _renderSalePayments(containerId,sale) — таблица всех оплат (initial+Оплаты), fully-paid badge, inline add-payment с валидацией; _addSalePayment(saleId) — через addPayment API; Block 3: is_installment/wallet_id/paid_kgs/_debt → create-only (showIf: _mode=create); sale_payments container; imei_search.renderEdit — read-only в edit; validate — skip purchase_id в edit, paid≤price в create; onSave — edit → updateSale; afterMount — edit mode: disabled has_imei, autoFillSelects, display fields, payments table, profit calc | canEdit:false блокировал редактирование; view показывал is_installment/wallet_id/paid_kgs как поля; viewActions через paymentForm вместо inline; onSave только addSale; afterMount без edit-ветки | **PageSales.html**: удалены `canEdit: function(){return false}` и `viewActions[]`; добавлены `_renderSalePayments(containerId,sale)` и `_addSalePayment(saleId)` перед saleForm; Block 3 fields: is_installment/wallet_id/paid_kgs/_debt — добавлен `editOnly:true, showIf: _mode=create`; добавлен `sale_payments` container; imei_search.renderEdit: ветка edit — read-only товар + hidden purchase_id; validate: skip purchase_id в edit, paid check в create; onSave: ветка editId → updateSale({id,buyer,wa,...}); afterMount: ветка edit — перенос manager_id, disabled has_imei, _sAutoFillSelects, display fields, _renderSalePayments, _sCalcProfit | - `PageSales.html` |
